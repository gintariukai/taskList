spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tasklist?currentSchema=tasklist&useUnicode=true&LegacyDatetimeCode=false&serverTimezone=UTC&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
      continue-on-error: false
      data-locations: classpath:demo_data.sql